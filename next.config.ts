/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  experimental: {
    // This might help with the workspace root issue
    // turbopackRoot: './',
  },
};

export default nextConfig;
